# KRONOS Aegis runs with NO required API keys — all data sources are free/public:
#   Scan   → GoPlus (keyless)
#   Depth  → DefiLlama (keyless)
#   Ledger → Base Blockscout (keyless) + DefiLlama prices + Frankfurter FX (all keyless)
COINGECKO_API_KEY=
